sb-cli - CLI command and options parser
=========

# Note to self:
I wrote this because Commander doesn't have a human-friendly sub-command API. and because minimist has no subcommand API and neither does meow.
