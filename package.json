{
  "name": "sb-command",
  "version": "2.0.6",
  "description": "CLI command and options parser",
  "main": "lib/index.js",
  "scripts": {
    "test": "(apm test) && (flow check) && (eslint . )",
    "clean": "rm -rf lib",
    "build": "npm run clean; babel src --out-dir lib",
    "watch": "npm run build -- -w"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/steelbrain/command"
  },
  "keywords": [
    "cli",
    "command",
    "app"
  ],
  "author": "steelbrain <i@steelbrain.me>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/steelbrain/command/issues"
  },
  "dependencies": {
    "camelcase": "^4.0.0",
    "cliff": "^0.1.10"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-steelbrain": "^5.0.2",
    "eslint-config-steelbrain": "^3.0.0",
    "flow-bin": "^0.54.0"
  }
}
